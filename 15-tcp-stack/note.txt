Note:

Question:
1. 什么时候要关服务器的tsk

Step:

Todo:


Debug:
1. tsk->snd_wnd==0未及时更新，死锁, syn_read就得更新了
2. Duplicated ACK包: 在established的普通ack|psh时不返回了
3. 服务器CLOSE_WAIT和FIN_WAIT_2死锁: 收到FIN包开始wake_up(recv)